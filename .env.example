# ===========================================
# HypnoRaffle Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# ===========================================
# PostgreSQL Configuration (Local Docker)
# ===========================================
POSTGRES_USER=raffle
POSTGRES_PASSWORD=raffle_secret_123
POSTGRES_DB=raffle_db

# JWT Secret for PostgREST (min 32 characters)
JWT_SECRET=super-secret-jwt-token-with-at-least-32-characters

# ===========================================
# App Configuration
# ===========================================
# PostgREST API URL (automatically set by Docker Compose)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: Webhook URL for winner notifications
NEXT_PUBLIC_WINNER_WEBHOOK_URL=

# ===========================================
# Cloudflare Tunnel (for secure internet exposure)
# ===========================================
# Get this from: https://one.dash.cloudflare.com/
# Create a tunnel and copy the token
CLOUDFLARE_TUNNEL_TOKEN=your_cloudflare_tunnel_token
